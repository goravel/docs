# 从 v1.17 升级到 v1.18

## 功能增强 🚀

- [AI SDK](#ai-sdk)
- [遥测](#遥测)

## 功能增强 🚀

- [Laravel 风格的集合](#laravel风格的集合)
- [原生验证引擎和内置规则](#原生验证引擎和内置规则)
- [验证支持基于翻译的错误信息和 `lang:publish` 命令](#验证支持基于翻译的错误信息和-lang-publish-命令)
- [维护模式命令](#维护模式命令)
- [运行器可以通过配置选项禁用](#运行器可以通过配置选项禁用)
- [路由的 WithoutMiddleware](#路由的-withoutmiddleware)
- [build 命令支持 go generate](#build-命令支持-go-generate)
- [Artisan 控制台支持表格输出](#artisan-控制台支持表格输出)
- [Artisan 控制台支持通过 Shutdownable 实现优雅关闭](#artisan-控制台支持通过-shutdownable-实现优雅关闭)
- [Artisan 控制台支持命令过滤](#artisan-控制台支持命令过滤)
- [gRPC 支持 TLS 和 mTLS 凭据](#grpc-支持-tls-和-mtls-凭据)
- [Orm 查询暴露上下文](#orm-查询暴露上下文)
- [通过 LoadViewsFrom 打包视图](#通过-loadviewsfrom-打包视图)
- [包设置支持导入别名](#包设置支持导入别名)
- [队列驱动支持通过 DriverWithReceive 进行批量消费](#队列驱动支持通过-driverwithreceive-进行批量消费)
- [日志 WithContext 过滤掉框架上下文键](#日志-withcontext-过滤掉框架上下文键)
- [安装程序支持代理技能管理命令](#安装程序支持代理技能管理命令)
- [Fiber 驱动上下文与 Fiber 实例生命周期解耦](#fiber-驱动上下文与-fiber-实例生命周期解耦)
- [Fiber 驱动模板配置被 DefaultTemplate 替换](#fiber-驱动模板配置被-defaulttemplate-替换)
- [缓存前缀支持空字符串](#缓存前缀支持空字符串)

## 破坏性变化 🛠

- [HTTP 中间件从 func 改为 interface](#http-中间件从-func-改为-interface)
- [验证引擎原生重写](#验证引擎原生重写)
- [邮件内容将纯文本与模板视图分离](#邮件内容将纯文本与模板视图分离)
- [gRPC 配置键从 servers 重命名为 clients](#grpc-配置键从-servers-重命名为-clients)
- [HTTP 驱动超时中间件为并发安全重写](#http-驱动超时中间件为并发安全重写)

## 升级指南

随着 [Golang v1.24 不再被维护](https://endoflife.date/go)，Goravel 支持的 Golang 版本已从 1.24 升级到 1.25。

goravel/example 项目从 v1.17 升级到 v1.18 的 PR 可作为升级参考：[goravel/example#133](https://github.com/goravel/example/pull/133)。

您可以复制以下内容粘贴到您的AI编码助手，以自动升级您的Goravel项目。

````markdown
# Upgrade Guide for Goravel v1.18

**Before you begin**: ensure all changes are committed or stashed, the project builds cleanly (`go build ./...`), and Go ≥ 1.25 is installed, Goravel version ≥ 1.17.0.

## Step 1: Update Go module dependencies

**Detection**: Check which facade packages are already installed:

```shell
rg -l 'goravel/(gin|fiber|redis|s3|oss|cos|minio)' go.sum 2>/dev/null
```

**Action**: Run the framework upgrade command and upgrade only the facade packages detected in `go.sum`:

```shell
go get github.com/goravel/framework@latest

# For each facade found in detection, run the corresponding go get.
# If goravel/gin is in go.sum:   go get github.com/goravel/gin@latest
# If goravel/fiber is in go.sum: go get github.com/goravel/fiber@latest
# If goravel/redis is in go.sum: go get github.com/goravel/redis@latest
# If goravel/s3 is in go.sum:    go get github.com/goravel/s3@latest
# If goravel/oss is in go.sum:   go get github.com/goravel/oss@latest
# If goravel/cos is in go.sum:   go get github.com/goravel/cos@latest
# If goravel/minio is in go.sum: go get github.com/goravel/minio@latest

go mod tidy
```

## Step 2: Migrate HTTP middleware from func to interface

`contracts/http.Middleware` changed from `func(http.Context)` to an interface with `Handle(http.Context)` and `Signature() string` methods. Every custom middleware must be updated.

### 2a. Detect all custom middleware functions

```shell
rg -n 'func\s+\w+\s*\(.*\)\s+http\.Middleware' --type go
rg -n 'func\s+\w+\s*\(.*\)\s+contractshttp\.Middleware' --type go
```

Also search for anonymous middleware (inline `return func(ctx http.Context)`):

```shell
rg -n 'return\s+func\(ctx\s+http\.Context\)' --type go
```

### 2b. Convert each middleware

For each detected middleware, keep the factory function signature and replace the closure body with a struct literal:

```diff
 func Auth() http.Middleware {
-    return func(ctx http.Context) {
-        ctx.Request().Next()
-    }
+    return &Auth{}
 }

+type Auth struct{}
+
+func (a *Auth) Handle(ctx http.Context) {
+    ctx.Request().Next()
+}
+
+func (a *Auth) Signature() string {
+    return "auth"
+}
```

If the original factory accepted parameters, pass them to the struct literal:

```diff
 func Throttle(name string) http.Middleware {
-    return func(ctx http.Context) {
-        // throttle logic using name
-    }
+    return &Throttle{Name: name}
 }
+
+type Throttle struct{ Name string }
+
+func (t *Throttle) Handle(ctx http.Context) { /* throttle logic using t.Name */ }
+func (t *Throttle) Signature() string { return "throttle-" + t.Name }
```

Call sites like `middleware.Auth()` stay unchanged — the factory function still returns `http.Middleware`, but now returns a struct instead of a closure.

### 2c. Verification

```shell
# Ensure no remaining func-returning middleware patterns
rg -n 'func.*http\.Middleware' --type go

# Build must succeed
go build ./...
```

## Step 3: Update validation rule and filter maps

Validation rules and filters now use `map[string]any` instead of `map[string]string`.

### 3a. Detect affected files

```shell
rg -n 'map\[string\]string.*Validate\(' --type go
rg -n 'map\[string\]string.*Make\(' --type go
rg -n 'Rules\(\).*map\[string\]string' --type go
rg -n 'Filters\(\).*map\[string\]string' --type go
rg -n 'Messages\(\).*map\[string\]string' --type go
rg -n 'Attributes\(\).*map\[string\]string' --type go
```

Also search for `Validate` and `Make` calls that may already use `map[string]any` or use short variable declarations:

```shell
rg -n '\.Validate\(.*map\[string\]' --type go
rg -n 'validation\.Make\(.*map\[string\]' --type go
```

### 3b. Apply the transformation

Replace `map[string]string` with `map[string]any` in `Validate` and `Make` calls:

```diff
-ctx.Request().Validate(map[string]string{
+ctx.Request().Validate(map[string]any{
     "title": "required|max:255",
 })
```

Also update form request method signatures — `Rules()`, `Messages()`, `Attributes()`, and `Filters()`:

```diff
-func (r *UserCreate) Rules(ctx http.Context) map[string]string {
-    return map[string]string{
+func (r *UserCreate) Rules(ctx http.Context) map[string]any {
+    return map[string]any{
         "name": "required",
     }
 }

-func (r *UserCreate) Messages(ctx http.Context) map[string]string {
-    return map[string]string{}
+func (r *UserCreate) Messages(ctx http.Context) map[string]any {
+    return map[string]any{}
 }

-func (r *UserCreate) Filters(ctx http.Context) map[string]string {
-    return map[string]string{
+func (r *UserCreate) Filters(ctx http.Context) map[string]any {
+    return map[string]any{
         "name": "trim",
     }
 }
```

If a `regex` or `not_regex` rule value contains `|` and is followed by more rules, switch to `[]string`:

```diff
 rules := map[string]string{
-    "slug": "required|regex:^(news|docs)-[a-z]+$|string",
+    "slug": []string{"required", "regex:^(news|docs)-[a-z]+$", "string"},
 }
```

### 3c. Verification

```shell
# Ensure no remaining map[string]string in validation contexts
rg -n 'Validate\(.*map\[string\]string' --type go
rg -n 'Make\(.*map\[string\]string' --type go
rg -n 'Rules\(.*map\[string\]string' --type go
rg -n 'Messages\(.*map\[string\]string' --type go
rg -n 'Filters\(.*map\[string\]string' --type go

go build ./...
```

## Step 4: Update mail template content fields

Mail content fields are now explicit: `HtmlView` for HTML templates, `TextView` for plain-text templates, `Html`/`Text` for raw body content.

### 4a. Detect affected files

```shell
rg -n '\.View\s*:' --type go -g '!vendor/**' -g '!storage/**'
rg -n 'Content\(.*mail\.Content\{' --type go
rg -n '&mail\.Content\{' --type go
```

### 4b. Apply the transformation

For each `mail.Content{}` literal:

| Old field | New field | Condition |
|-----------|-----------|-----------|
| `View:` | `HtmlView:` | Always (was for HTML templates) |
| `Text:` (value ends in `.html`, `.txt`, `.tmpl`, or is a file path) | `TextView:` | When the value was a template path |
| `Text:` (value is a raw string, not a path) | `Text:` | Keep as-is for raw body |

**Example — template-based email**:
```diff
 Content(mail.Content{
-    View: "welcome.html",
-    Text: "welcome.txt",
+    HtmlView: "welcome.html",
+    TextView: "welcome.txt",
     With: map[string]any{
         "Name": "Goravel",
     },
 })
```

**Example — raw body email (no change)**:
```go
Content(mail.Content{Text: "Hello Goravel"})
```

If a file uses `Html:` / `Text:` already (raw body pattern from v1.17+), no change is needed.

### 4c. Verification

```shell
# Ensure no remaining View: in mail Content structs
rg -n '\.View\s*:.*\.html' --type go

go build ./...
```

## Step 5: Rename gRPC config key servers to clients

### 5a. Detect affected files

```shell
rg -n '"servers"' --type go -g '*grpc*'
rg -n 'grpc\.servers' --type go
```

### 5b. Apply the transformation

In `config/grpc.go`, rename the config key:

```diff
 config.Add("grpc", map[string]any{
     "host": config.Env("GRPC_HOST", ""),
-    "servers": map[string]any{
+    "clients": map[string]any{
         "user": map[string]any{
             "host":           config.Env("GRPC_USER_HOST", ""),
             ...
         },
     },
 })
```

In application code, update any config path references:

```diff
-facades.Config().Get("grpc.servers.user.host")
+facades.Config().Get("grpc.clients.user.host")
```

### 5c. Verification

```shell
# Ensure no remaining "servers" key in grpc config
rg -n '"servers"' --type go -g '*grpc*'
rg -n 'grpc\.servers' --type go

go build ./...
```

## Step 6: Update validation error messages and custom rule names

The new native validation engine ([goravel/framework#1397](https://github.com/goravel/framework/pull/1397)) produces Laravel-style error messages and evaluates all rules (including wildcards) differently. Any tests that assert on specific validation error messages or JSON error responses must be updated.

### 6a. Detect affected tests

```shell
rg -n 'Equal.*message.*required' --type go
rg -n 'Equal.*message.*regex' --type go
rg -n 'Equal.*message.*date' --type go
rg -n 'Required.*is required to not be empty' --type go
```

### 6b. Update error message expectations

Replace old-style error message assertions with the new Laravel-style messages:

| Old message | New message |
|-------------|-------------|
| `"name is required to not be empty"` | `"The name field is required."` |
| `"code value does not pass the regex check"` | `"The code field format is invalid."` |
| `"date value should be a date string"` | `"The date field must be a valid date."` |

Example test diff:

```diff
-s.Equal("{\"message\":{\"name\":{\"required\":\"name is required to not be empty\"}}}", content)
+s.Equal("{\"message\":{\"name\":{\"required\":\"The name field is required.\"}}}", content)
```

> **Note**: Wildcard rules (e.g., `scores.*`, `tags.*`) now consistently appear in error output even when no input is provided. Add assertions for these if your tests check the full error response body.

### 6c. Rename custom rules that conflict with built-in rules

The new engine includes built-in rules (`required`, `email`, `exists`, `unique`, etc.) that may conflict with custom rule names. Rename any custom rule whose `Signature()` matches a built-in rule name:

```diff
 func (receiver *Exists) Signature() string {
-    return "exists"
+    return "exists_custom"
 }
```

### 6d. Verification

```shell
go build ./...
go test ./... 2>&1 || true
```

## Step 7: Install the goravel-development skill

Download the Goravel AI coding conventions skill from goravel-lite so you can reference project structure, facades, testing patterns, and Artisan commands during the upgrade:

```shell
mkdir -p .agents/skills/goravel-development
curl -fsSL -o .agents/skills/goravel-development/SKILL.md \
  https://raw.githubusercontent.com/goravel/goravel-lite/master/.agents/skills/goravel-development/SKILL.md
```

### 7a. Verification

```shell
ls -l .agents/skills/goravel-development/SKILL.md
```

## Step 8: Final verification

After completing all steps, run the full verification suite:

```shell
go build ./...
go vet ./...
go test ./... 2>&1 || true
```

If tests fail, the most common causes are:
- Test middleware functions that were not migrated (step 2).
- Test validation calls using `map[string]string` (step 3).
- Test mail Content structs with old field names (step 4).
- Test validation error message assertions with old format (step 6).
- Custom rule signatures conflicting with built-in rule names (step 6).
````

## 功能介绍

### AI 开发工具包

Goravel v1.18 通过一个外观引入了用于对话、附件、生成媒体和语音工作流的官方 AI SDK。 使用 `go run .artisan package:install AI` 安装外观，然后安装一个提供程序包，例如 `goravel/openai`、`goravel/anthropic` 或 `goravel/gemini`。

初始版本包括：

- 智能体对话，包括 `make:agent`、`make:tool`、可定制的 AI 生成器路径、`Prompt`、`Stream`、工具调用、提示中间件、提供程序故障转移。
- 请求范围内的文档和图像附件，支持字节、读取器、路径、存储、URL、上传或提供程序管理的文件 ID。
- 使用 `Store`/`StoreAs` 辅助程序生成图像和音频，以及带有语言和说话人分离选项的转录。

`goravel-lite` 脚手架还附带了一个 _AI 智能体开发技能_，用于教授 AI 编码智能体（Cursor、Copilot、Codex 等） 如何使用 Goravel 项目。 该技能涵盖项目结构、外观、路由、ORM、测试约定和 Artisan 脚手架命令。 当 AI 代理读取此技能时，它已经知道如何引导 Goravel 项目，使用 `package:install` 添加外观，使用外观模式，使用 `testify` 套件和模拟助手编写测试，以及使用 Artisan `make:*` 生成器。 您可以通过创建 `.agents/skills/goravel-development/CUSTOM.md` 文件来使用项目特定规则扩展此技能。 此技能与 `goravel-lite` 一起版本化，并在您升级框架时更新。

一个最小化的提示如下所示：

```go
conversation, err := facades.AI().Agent(&agents.SupportAgent{})
if err != nil {
    return err
}

response, err := conversation.Prompt(\"How do I create a controller?\")
if err != nil {
    return err
}

fmt.Println(response.Text())
```

[查看文档](../ai/sdk.md)

### 遥测

Goravel v1.18 引入了一个基于 OpenTelemetry 的遥测模块，用于收集追踪、指标和日志，并将其导出到任何兼容 OTLP 的后端，例如 Jaeger、Prometheus、Grafana 或 Datadog。 使用 `go run . 安装外观。 artisan package:install Telemetry`。

初始版本包括：

- 从单个 `config/telemetry.go` 文件配置的追踪、指标和日志，支持 OTLP、控制台和自定义导出器、采样和批处理控制。
- 为 HTTP 服务器、HTTP 客户端、gRPC、数据库和日志记录器提供内置检测，支持跨服务的跟踪上下文传播。
- 当应用程序停止时自动刷新和关闭。

手动 span 示例如下：

```go
ctx, span := facades.Telemetry().Tracer("app").Start(ctx, "process-order")
defer span.End()

span.SetAttributes(telemetry.String("order.id", "1234"))

// 将 ctx 向下传递以创建子 span
processItems(ctx)
```

`Tracer` 参数 (`"app"`) 是检测范围名称，它标识产生 span 的代码；它会被记录为 `otel.scope.name`。 按照惯例，它是包导入路径，对于应用程序代码，则是应用程序名称。

详情请 [查看文档](../digging-deeper/telemetry.md)

### Laravel 风格的集合

Goravel 现在在 `support/collect` 包中提供了流畅的即时和惰性集合工具。 您可以从切片创建集合，链式过滤和映射操作，对结构体使用 Laravel 风格的 `Where` 比较，聚合值，并使用 `LazyCollection` 处理大型数据集。

```go
numbers := collect.New(1, 2, 3, 4, 5, 6)

evens := numbers.Filter(func(n int, _ int) bool {
    return n%2 == 0
})

evens.All() // []int{2, 4, 6}
```

详情请 [查看文档](../digging-deeper/collections.md)

### 原生验证引擎和内置规则

Goravel 现在使用原生验证引擎，取代了之前的外部验证依赖。 新引擎支持点符号、通配符验证、验证数据检索、自定义过滤器、显式消息优先级以及更广泛的 Laravel 风格内置规则集。

```go
validator, err := facades.Validation().Make(ctx,
    map[string]any{
        \"email\": \" User@Goravel.dev \",
        \"scores\": []int{1, 2},
    },
    map[string]any{
        \"email\": \"required|email\",
        \"scores.*\": \"required|integer\",
    },
    validation.Filters(map[string]any{
        \"email\": \"trim|lower\",
    }),
)

validated := validator.Validated()
scores := validated[\"scores\"].([]int)
```

[查看文档](../the-basics/validation.md)

### 验证支持基于翻译的错误消息和 lang:publish 命令

验证错误消息现在由框架的[本地化](../digging-deeper/localization.md)系统提供支持。 默认消息嵌入为 `lang/en/validation.json`，并使用 `validation.*` 翻译键（例如 `validation.required`、`validation.min.string`）。

使用新的 `lang:publish` Artisan 命令将验证语言文件发布到您的应用程序：

```shell
go run . artisan lang:publish
go run . artisan lang:publish --force
```

发布后，编辑 `lang/en/validation.json` 以自定义默认错误消息。 为其他语言环境创建额外的语言目录（例如 `lang/zh/validation.json`）。 自定义规则消息现在支持 `:value` 和 `:optionN` 占位符。

[查看文档](../the-basics/validation.md#translation--localization)

### 维护模式命令

Goravel 现在提供了 `artisan down` 和 `artisan up` 命令用于应用维护模式。 默认使用 `file` 维护驱动器，因此 `down` 命令将维护元数据写入存储，`up` 命令则将其移除。

```shell
go run . artisan down
go run . artisan up
```

`down` 命令支持通过选项自定义响应，例如 `--reason`、`--status`、`--redirect`、`--render`、`--secret` 和 `--with-secret`。

```shell
go run . artisan down --reason="正在升级数据库" --status=503
go run . artisan down --redirect=/maintenance
go run . artisan down --render=errors/503
go run . artisan down --with-secret
```

维护设置从 `config/app.go` 中的 `app.maintenance` 读取。 新路由安装会自动添加此配置。 现有应用程序可以手动添加：

```go
"maintenance": map[string]any{
    "driver": config.Env("APP_MAINTENANCE_DRIVER", "file"),
    "store":  config.Env("APP_MAINTENANCE_STORE", ""),
},
```

对于多服务器部署，设置 `APP_MAINTENANCE_DRIVER=cache` 将维护状态存储在共享缓存存储中。 使用 `APP_MAINTENANCE_STORE` 选择指定的缓存存储，例如 Redis：

```ini
APP_MAINTENANCE_DRIVER=cache
APP_MAINTENANCE_STORE=redis
```

[查看文档](../getting-started/configuration.md#maintenance-mode)

### 可以通过配置选项禁用运行器

Goravel v1.18 将内部的 `app.auto_run` 标志替换为公开的、支持通配符的 `app.disabled_runners` 配置选项。 它允许您在主进程中有选择地跳过自动运行的[运行器](../architecture-concepts/service-providers.md#runners)，同时保持其他服务运行——例如，在专用容器上运行调度程序而不禁用HTTP服务器，或通过跳过`goravel:http`、`goravel:queue`和`goravel:grpc`运行器构建仅调度容器。

该值是一个glob模式切片，使用Go的[`path.Match`](https://pkg.go.dev/path#Match)匹配每个运行器的签名：

```go
// 配置/app.go
"app": map[string]any{
    "env":   "production",
    "debug": false,
    "disabled_runners": []string{"goravel:schedule"},
},
```

框架运行器签名有`goravel:http`、`goravel:grpc`、`goravel:queue`、`goravel:schedule`和`goravel:telemetry`。 模式按顺序评估，第一个匹配优先，因此您可以混合精确匹配和通配符匹配：

```go
// Web容器 — 仅跳过调度程序
"disabled_runners": []string{"goravel:schedule"},

// 仅调度容器 — 禁用http、queue、grpc
"disabled_runners": []string{"goravel:http", "goravel:queue", "goravel:grpc"},

// 禁用所有框架运行器
"disabled_runners": []string{"goravel:*"},

// 终止开关：在主进程中不运行任何自动运行器
"disabled_runners": []string{"*"},
```

无效模式会记录为警告，运行者保持运行，因此配置中的拼写错误不会导致启动崩溃。 过滤器在 `foundation.configureRunners()` 中集中运行，因此未来添加的任何新运行者都会自动获得相同行为。

[查看文档](../getting-started/configuration.md#disabled-runners)

### 为路由提供 WithoutMiddleware

Goravel v1.18 为路由和路由组引入了 `WithoutMiddleware` 方法，类似于 Laravel 的 `withoutMiddleware()`。 这允许特定路由绕过父组通常会应用的某些中间件——对于 webhook 端点、公共 API 或应跳过身份验证或速率限制的路由非常有用。

在中间件组内排除单个路由上的中间件：

```go
facades.Route().Middleware(middleware.Auth(), middleware.Throttle("global")).Group(func(router route.Router) {
  router.Get("/dashboard", dashboardController.Index)

  // 此路由排除节流中间件
  router.Get("/api/webhook", webhookController.Handle).
    WithoutMiddleware(middleware.Throttle("global"))
})
```

排除组内所有路由的中间件：

```go
facades.Route().Middleware(middleware.Auth()).
  WithoutMiddleware(middleware.Auth()).
  Group(func(router route.Router) {
    router.Get("/public", publicController.Index)
  })
```

> **注意**：中间件排除使用 `Signature()` 方法来识别中间件。 确保每个中间件返回唯一的签名，以便 `WithoutMiddleware` 正常工作。 内置框架中间件已提供唯一签名。

[查看文档](../the-basics/routing.md#无中间件)

### Build 命令支持 go generate

`build` 命令现在支持 `--generate` 标志和 `-g` 别名。 启用后，Goravel 在编译二进制文件之前运行 `go generate ./...`，使得在不改变默认行为的情况下更容易在构建工作流中包含生成的代码。

```shell
go run . artisan build --generate
go run . artisan build -g
```

[查看文档](../getting-started/compile.md#编译命令)

### Artisan 控制台支持表格输出

Goravel 现在提供了 `ctx.Table` 方法来渲染结构化命令输出。 它接受表头、表行，以及可选的 `console.TableOption` 用于自定义边框、样式、列、宽度和高度。

```go
func (receiver *ReportCommand) Handle(ctx console.Context) error {
    headers := []string{"ID", "Name", "Status"}
    rows := [][]string{
        {"1", "Goravel", "Active"},
        {"2", "Framework", "Ready"},
    }

    ctx.Table(headers, rows)

    return nil
}
```

详情请 [查看文档](../digging-deeper/artisan-console.md#tables)

### Artisan 控制台通过 Shutdownable 支持优雅关闭

控制台命令现在可以通过实现可选的 `console.Shutdownable` 接口来选择优雅关闭。 框架通过每个命令连接一个 `signal.NotifyContext`，因此按 `Ctrl+C`（或发送 `SIGTERM`）会取消传递给 `Handle` 的上下文。 如果命令还实现了 `Shutdown(ctx Context) error`，框架会使用新的上下文调用它，并在进程退出前给予 30 秒的清理时间。 `Handle` 在 goroutine 中运行，因此信号响应是即时的。

`console.Context` 现在嵌入了 `context.Context`，因此命令可以直接使用 `<-ctx.Done()`，将 `ctx` 传递给期望 `context.Context` 的函数，并调用 `ctx.Deadline()` / `ctx.Err()` / `ctx.Value(key)` 而无需访问器。 `schedule:run` 命令是典型示例——它实现了 `Shutdown` 以将清理转发给 `schedule.Shutdown(ctx)`。

```go
package commands

import (
  "errors"
  "net/http"

  "github.com/goravel/framework/contracts/console"
  "github.com/goravel/framework/contracts/console/command"
)

type Serve struct {
  server *http.Server
}

func (r *Serve) Signature() string   { return "serve" }
func (r *Serve) Description() string { return "Start the HTTP server" }
func (r *Serve) Extend() command.Extend {
  return command.Extend{Category: "server"}
}

func (r *Serve) Handle(ctx console.Context) error {
  if err := r.server.ListenAndServe(); err != nil && !errors.Is(err, http.ErrServerClosed) {
    return err
  }
  return nil
}

func (r *Serve) Shutdown(ctx console.Context) error {
  ctx.Info("received signal, shutting down gracefully...")
  return r.server.Shutdown(ctx)
}
```

未实现 `Shutdownable` 的命令保持原始行为——收到信号后进程立即退出。

[查看文档](../digging-deeper/artisan-console.md#graceful-shutdown)

### Artisan 控制台支持命令过滤

`ApplicationBuilder` 现在提供了一个 `WithCommandsFilter` 方法来限定将哪些内置的 Artisan 命令注册 — 对于不需要 `make:*`、`package:*` 或 `vendor:publish` 的生产二进制文件很有用。 回调返回一个要保留的命令签名的正向列表，支持完全匹配和通配符（`*`）匹配：

```go
func Boot() contractsfoundation.Application {
	return foundation.Setup().
		WithCommands(Commands).
		WithCommandsFilter(func() []string {
			if facades.Config().GetString("app.env") == "production" {
				return []string{
					"up", "down", "key:generate", "about",
					"schedule:*",
					"queue:*",
				}
			}
			return nil // 在其他环境中保留所有内容
		}).
		WithConfig(config.Boot).
		Create()
}
```

[查看文档](../digging-deeper/artisan-console.md#filtering-commands)

### gRPC 支持 TLS 和 mTLS 凭据

Goravel 现在支持在应用程序启动期间注册 gRPC 服务器和客户端传输凭据。 使用 `WithGrpcServerCredentials` 为服务器启用 TLS 或 mTLS，使用 `WithGrpcClientCredentials` 注册客户端凭据组，可通过 `grpc.clients.<name>.credentials`。

```go
return foundation.Setup().
    WithGrpcClientCredentials(func() map[string]credentials.TransportCredentials {
        userCreds, err := credentials.NewClientTLSFromFile("storage/certs/ca.crt", "user.example.com")
        if err != nil {
            panic(err)
        }

        return map[string]credentials.TransportCredentials{
            "user": userCreds,
        }
    }).
    Create()
```

[查看文档](../the-basics/grpc.md#transport-credentials)

### Orm 查询暴露上下文

Orm 查询现在提供了一个 `Context()` 访问器。 接收 `contractsorm.Query` 的代码，例如模型全局作用域，可以读取由 `facades.Orm().WithContext(ctx)` 传递的上下文。

```go
"tenant": func(query contractsorm.Query) contractsorm.Query {
    tenantID, _ := query.Context().Value("tenant_id").(uint)

    return query.Where("tenant_id", tenantID)
}
```

[查看文档](../orm/getting-started.md#setting-global-scope)

### 通过 LoadViewsFrom 加载包视图

扩展包现在可以使用 `facades.View().LoadViewsFrom(path)` 注册自己的视图目录。 应用程序的 `resources/views` 目录优先级最高，注册的包视图作为默认值。 `Exist` 方法也会检查注册的包视图路径。

```go
// In an extension package's service_provider.go
func (r *ServiceProvider) Boot(app foundation.Application) {
    facades.View().LoadViewsFrom("/path/to/package/views")
}
```

[查看文档](../the-basics/views.md#registering-views-from-packages)

> **注意**：如果你想在 fiber 驱动中使用此功能，请查看 [Fiber 驱动模板配置已替换为 DefaultTemplate](#fiber-驱动模板配置已替换为-defaulttemplate)。

### 包设置支持导入别名

包设置修改辅助函数现在支持 `"<alias> <import-path>"` 格式的导入别名。 这可以在包导入路径较长或需要以自定义名称引用包时，使生成的引导文件保持可读性。

```go
serviceProvider := "&admin.ServiceProvider{}"
moduleImport := "admin " + setup.Paths().Module().Import()

setup.Install(
    modify.RegisterProvider(moduleImport, serviceProvider),
).Uninstall(
    modify.UnregisterProvider(moduleImport, serviceProvider),
).Execute()
```

详情请 [查看文档](../digging-deeper/package-development.md#install-the-package)

### 队列驱动支持通过 DriverWithReceive 批量消费

自定义队列驱动现在可以有选择地实现 `DriverWithReceive` 接口（[goravel/framework#1435](https://github.com/goravel/framework/pull/1435)），以支持阻塞式批量消息消费。 当驱动实现此接口时，队列工作器会自动使用 `Receive` 而不是 `Pop`，从而减少像 Kafka 或 RabbitMQ 这样的高吞吐量驱动的轮询开销。

```go
import (
  "context"
  "github.com/goravel/framework/contracts/queue"
)

type KafkaDriver struct{}

// Base Driver interface methods (required)
func (d *KafkaDriver) Driver() string                        { return "kafka" }
func (d *KafkaDriver) Push(task queue.Task, queue string) error { return nil }
func (d *KafkaDriver) Pop(queue string) (queue.ReservedJob, error) { return nil, nil }

// Optional DriverWithReceive for batch consumption
func (d *KafkaDriver) Receive(ctx context.Context, queue string, count int) ([]queue.ReservedJob, error) {
  // blocking batch receive up to `count` jobs, returns when ctx expires
}
```

当 `Receive` 可用时，工作器将运行一个阻塞批量循环，每次调用超时5秒，并在错误或空批次时使用指数退避（100ms–3.2s）。 `context.Context` 在工作器关闭时被取消，确保干净终止。 现有驱动程序无需更改应用程序代码。

详情请 [查看文档](../digging-deeper/queues.md#batch-receive-drivers)

### Log WithContext 过滤掉框架上下文键

之前，`facades.Log().WithContext(ctx)` 可能会将框架内部上下文键如 `GoravelAuthJwt` 和 `goravel_http_client_name` 写入日志输出。 Goravel 现在默认排除这些键，您可以添加 `logging.context.exclude` 用于项目特定的上下文键。

[查看文档](../the-basics/logging.md#inject-context)

### 安装程序支持智能体技能管理命令

`goravel` CLI 现在支持 `goravel skill:list` 和 `goravel skill:install`，用于发现和安装 Goravel 智能体技能。 这些命令专为想要安装 AI 编码助手技能以帮助遵循 Goravel 开发规范如测试、命名和拉取请求工作流的贡献者设计。

技能来源于 [goravel/agents](https://github.com/goravel/agents) 仓库，默认安装到 `~/.agents/skills`。 您可以按名称筛选，使用 `--path` 选择自定义目标，并使用 `--force` 覆盖现有技能。

[查看文档](../prologue/contributions.md#agent-skills)

### Fiber 驱动程序上下文与 Fiber 实例生命周期解耦

当使用 Fiber 驱动时请求超时，`context.Context` 接口方法（`Done()`、`Deadline()`、`Err()`）委托给底层的 `fiber.Ctx`，而该对象在请求被 fasthttp 回收后变得无效。 这导致当后台 goroutine（例如 `database/sql`）仍持有请求上下文的引用时，出现空指针 panic（[goravel/goravel#951](https://github.com/goravel/goravel/issues/951)）。

修复方法（[goravel/fiber#253](https://github.com/goravel/fiber/pull/253)）将用户上下文和值作为结构体字段存储在 goravel 的 `Context` 结构体中，而不是委托给 `fiber.Ctx`，因此超时信号能够正确传播，后台 goroutine 可以在请求返回后安全地访问上下文。

无需修改应用程序代码。

### Fiber 驱动模板配置替换为 DefaultTemplate

为了通过 `LoadViewsFrom` 支持包视图，Fiber 驱动（[goravel/fiber#273](https://github.com/goravel/fiber/pull/273)）已将依赖从 `gofiber/template/html/v3` 替换为内置的 `DefaultTemplate()`，该模板会自动从 `resources/views` 和任何已注册的包视图加载视图。 如果您的 `config/http.go` 使用了自定义的 `template` 配置，使用 `html.New(path.Resource("views"), ".tmpl")`，并且您想使用[通过 LoadViewsFrom 使用包视图](#通过-loadviewsfrom-使用包视图)功能，请将其更新为使用 `goravelfiber.DefaultTemplate()`：

```diff
 // config/http.go — fiber 驱动

import goravelfiber "github.com/goravel/fiber"

 "template": func() (fiber.Views, error) {
-    return html.New(path.Resource("views"), ".tmpl"), nil
+    return goravelfiber.DefaultTemplate(), nil
 },
```

对于自定义分隔符或 FuncMap，请使用 `goravelfiber.NewTemplate(goravelfiber.RenderOptions{...})`：

```go
import goravelfiber "github.com/goravel/fiber"

"template": func() (fiber.Views, error) {
    return goravelfiber.NewTemplate(goravelfiber.RenderOptions{
        Delims: &goravelfiber.Delims{Left: "{[", Right: "]}"},
    })
},
```

如果没有定义 `template` 配置，则会自动使用 `DefaultTemplate()` — 无需更改代码。

### 缓存前缀支持空字符串

Redis 缓存驱动现在尊重空的 `prefix` 配置。 以前，即使前缀为空，冒号（`:`）分隔符也总是附加到每个键前，导致直接访问 Redis 时键不一致。 现在，如果 `config/cache.go` 中的 `prefix` 选项设置为空字符串，则不应用任何前缀或分隔符：

```go
// config/cache.go
"prefix": "",
```

使用此配置，缓存键如 `users` 在 Redis 中存储为 `users`，而不是 `:users`。

对于具有非空前缀的现有项目，无需更改应用程序代码。

[查看文档](../digging-deeper/cache.md#查看文档)

### HTTP 中间件从函数更改为接口

`contracts/http.Middleware` 已从函数类型 `func(Context)` 更改为接口：

```go
type Middleware interface {
    Handle(Context)
    Signature() string
}
```

此更改通过 `Signature()` 方法为每个中间件提供稳定的标识，使 `WithoutMiddleware` 能够通过签名比较可靠地排除特定中间件。 所有内置框架中间件（`Cors`、`Timeout`、`Throttle`、`VerifyCsrfToken` 等） 已更新为返回实现此接口的结构体。

自定义中间件必须更新以实现新接口。 参见[升级指南](#升级指南)了解迁移步骤。

[查看文档](../the-basics/middleware.md)

### 验证引擎本地重写

验证引擎在 [goravel/framework#1397](https://github.com/goravel/framework/pull/1397) 中被重写——用原生实现替换了外部 `gookit/validate` 依赖，该实现与 Laravel 的验证 API 完全兼容。 此更改引入了多个下游影响：

1. **错误消息格式已更改**，从旧式消息（例如 `"name is required to not be empty"`）改为 Laravel 风格的消息（例如 `"The name field is required."`）。 依赖特定验证错误消息的测试必须更新。

2. **通配符规则 (`*.`) 现在一致地报告错误。** 旧引擎可能根据输入跳过通配符字段错误；新引擎始终评估并报告它们。

3. **表单请求方法签名已更改** — `Rules()`、`Messages()`、`Attributes()` 和 `Filters()` 现在返回 `map[string]any` 而不是 `map[string]string`。

4. **自定义验证规则名称可能与新的内置规则冲突**。 重命名与内置规则同名的自定义规则（例如，`exists` → `exists_custom`）。

错误消息现在由[本地化](../digging-deeper/localization.md)系统提供。 默认消息嵌入为 `lang/en/validation.json`，并使用 `validation.*` 翻译键。 使用 `go run 。 artisan lang:publish` 来发布和自定义它们。

[查看文档](../the-basics/validation.md)

### 邮件内容将纯文本与模板视图分离

Goravel 现在将 `mail.Content.Text` 视为原始的纯文本邮件正文内容。 模板路径是明确的：使用 `HtmlView` 用于 HTML 模板，使用 `TextView` 用于纯文本模板。

当消息正文已经可用时，使用 `Html` 和 `Text`：

```go
func (m *WelcomeMail) Content() *mail.Content {
    return &mail.Content{
        Html: "<h1>Hello Goravel</h1>",
        Text: "Hello Goravel",
    }
}
```

当消息正文应该从模板渲染时，使用 `HtmlView` 和 `TextView`：

```go
func (m *WelcomeMail) Content() *mail.Content {
    return &mail.Content{
        HtmlView: "welcome.html",
        TextView: "welcome.txt",
        With: map[string]any{
            "Name": "Goravel",
        },
    }
}
```

详情请 [查看文档](../digging-deeper/mail.md#using-template)

### gRPC 配置键从 servers 重命名为 clients

gRPC配置键`servers`已在`config/grpc.go`中重命名为`clients`，以准确反映这些条目注册的是gRPC客户端（远程服务的消费者）而非服务端。

在`config/grpc.go`中，重命名顶级键：

```diff
 config.Add("grpc", map[string]any{
     "host": config.Env("GRPC_HOST", ""),
-    "servers": map[string]any{
+    "clients": map[string]any{
         "user": map[string]any{
             "host": config.Env("GRPC_USER_HOST", ""),
             ...
         },
     },
 })
```

在应用程序代码中，将所有配置路径引用从`grpc.servers`更新为`grpc.clients`：

```diff
-facades.Config().Get("grpc.servers.user.host")
+facades.Config().Get("grpc.clients.user.host")
```

[查看文档](../the-basics/grpc.md)

### HTTP驱动超时中间件针对并发安全性进行了重写

在Gin驱动（[goravel/gin#222](https://github.com/goravel/gin/pull/222)）和Fiber驱动（[goravel/fiber#263](https://github.com/goravel/fiber/pull/263)）中的超时中间件已被重写，以修复一个并发错误（[goravel#966](https://github.com/goravel/goravel/issues/966)）。 之前的实现将处理链在后台goroutine中运行，并可能在处理器继续运行在已可重用的请求范围状态时返回超时响应，导致高并发下的跨请求响应污染。

两个驱动现在都委托给各自框架的原生超时中间件（Gin 使用 `gin-contrib/timeout`，Fiber 使用内置超时），这些中间件会正确地放弃超时的请求上下文，而不是回收它们。 `Timeout(...)` 中间件签名保持不变，`ctx.Done()` 仍然反映请求截止时间——无需修改任何应用程序代码。

[查看文档](../the-basics/routing.md)
